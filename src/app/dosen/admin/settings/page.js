import AdminSettingsPanel from "@/components/AdminSettingsPanel";

export const metadata = {
  title: "Pengaturan Sistem - Portal Admin",
};

export default function AdminSettingsPage() {
  return <AdminSettingsPanel />;
}
